## Project from boot.dev: Build a Web Crawler in Go

### Usage

```go
go run . <website url> <max allowed goroutines> <max pages to crawl>
```
Out of the three command line arguments, only \<website url\> is required.

